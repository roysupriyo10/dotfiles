# live Claude Code state stays ignored; only the shared statusline files are tracked
/.claude/*
!/.claude/settings.shared.json
!/.claude/install.sh
!/.claude/themes/
.cursor/clip/

# accidental unexpanded ~ paths from broken install runs
/**/~
/~

.local/bin/mimo-claude.env

sway/config.d/variables.d/04-monitor-positions.conf

.DS_Store

# per-appearance symlink managed by appearance-sync (tmux dir is LINKed, not mirrored)
/tmux/theme.conf
