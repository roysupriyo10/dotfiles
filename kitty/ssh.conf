# kitten ssh — ~/.config/kitty/ssh.conf ONLY (never ~/.ssh/ssh.conf).
# https://sw.kovidgoyal.net/kitty/kittens/ssh/
#
# remote_kitty bootstraps kitten on the remote (clipboard, transfer, kitten @).
# Old invalid key was copy_kitten — causes "Ignoring bad config line" from kitten ssh.
remote_kitty if-needed
