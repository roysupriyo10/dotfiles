### Applications Configuration
# Quick access to applications and webapp floating windows

# Application launcher keybindings
# Basic applications already covered in 40-keybindings.conf

# Floating webapp windows (uncomment to use)
# bindsym $mod+Alt+t exec $browser --new-window --app=https://twitter.com
# bindsym $mod+Alt+d exec $browser --new-window --app=https://discord.com/app
# bindsym $mod+Alt+w exec $browser --new-window --app=https://web.whatsapp.com
# bindsym $mod+Alt+m exec $browser --new-window --app=https://mail.google.com
# bindsym $mod+Alt+n exec $browser --new-window --app=https://notion.so
# bindsym $mod+Alt+h exec $browser --new-window --app=https://github.com
# bindsym $mod+Alt+o exec $browser --new-window --app=https://chatgpt.com
# bindsym $mod+Alt+y exec $browser --new-window --app=https://youtube.com
