### Core Variables
# Essential key bindings and navigation

# Modifier key (Logo/Super key, use Mod1 for Alt)
set $mod Mod4

# Home row direction keys (vim-style)
set $left h
set $down j
set $up k
set $right l
